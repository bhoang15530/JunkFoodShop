    <!-- breadcrumb-section -->
<div class="breadcrumb-section breadcrumb-bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 text-center">
                <div class="breadcrumb-text">
                    <p>Fresh and Organic</p>
                    <h1>Cart</h1>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end breadcrumb section -->
<!-- cart -->
@if (ViewBag.CartList.Count <= 0)
{
    <div class="container">
        <h2>There's nothing in your cart! Go back to Homepage and continue shopping</h2>
        <a asp-action="Index" asp-controller="Home">Go back to Homepage</a>
    </div>
}
else
{
    <div class="cart-section mt-150 mb-150">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-md-12">
                    <div class="cart-table-wrap">
                        <table class="cart-table">
                            <thead class="cart-table-head">
                                <tr class="table-head-row">
                                    <th class="product-remove"></th>
                                    <th class="product-image">Product Image</th>
                                    <th class="product-name">Name</th>
                                    <th class="product-price">Price</th>
                                    <th class="product-quantity">Quantity</th>
                                    <th class="product-total">Total</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var cart in ViewBag.CartList)
                                {
                                    <tr class="table-body-row">
                                        <td class="product-remove"><a asp-action="RemoveFromCart" asp-controller="User" asp-route-fid="@cart.FoodId"><i class="far fa-window-close"></i></a></td>
                                        <td class="product-image"><img src="~/assets/img/products/product-img-1.jpg" alt="@cart.FoodName"></td>
                                        <td class="product-name">@cart.FoodName</td>
                                        <td class="product-price">@cart.FoodPrice.ToString("n0") ₫</td>
                                        <td class="product-quantity" data-quantity="@cart.FoodId"><input class="quantity-input-@cart.FoodId" type="number" min="1" data-foodid="@cart.FoodId" data-price="@cart.FoodPrice" value="@cart.Quantity"></td>
                                        <td class="product-subtotal-@cart.FoodId" data-subtotal="@cart.FoodId">@((cart.Quantity * cart.FoodPrice).ToString("n0")) ₫</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

                <div class="col-lg-4">
                    <div class="total-section">
                        <table class="total-table">
                            <thead class="total-table-head">
                                <tr class="table-total-row">
                                    <th>Total</th>
                                    <th>Price</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="total-data">
                                    <td><strong>Subtotal: </strong></td>
                                    <td class="subtotal"></td>
                                </tr>
                                <tr class="total-data">
                                    <td><strong>Total: </strong></td>
                                    <td class="total"></td>
                                </tr>
                            </tbody>
                        </table>
                        <div class="cart-buttons">
                            <a asp-action="CheckOut" asp-controller="User" class="boxed-btn black">Check Out</a>
                        </div>
                    </div>

                    <div class="coupon-section">
                        <h3>Apply Coupon</h3>
                        <div class="coupon-form-wrap">
                            <form action="index.html">
                                <p><input type="text" placeholder="Coupon"></p>
                                <p><input type="submit" value="Apply"></p>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- end cart -->
}
<script src="https://code.jquery.com/jquery-3.6.4.min.js" integrity="sha256-oP6HI9z1XaZNBrJURtCoUT5SUnxFr8s3BzRl+cbzUq8=" crossorigin="anonymous"></script>
<script>
    $(document).ready(function () {
        $("input[class^='quantity-input-']").each(function (index, element) {
            $(element).css("backgroundColor", "yellow");
            $(element).change(function () {
                var foodId = $(this).attr("data-foodid");
                var quantity = $(this).val();
                var price = $(this).attr("data-price");
                var subTotalEls = $(`td.product-subtotal-${foodId}`);
                console.log(quantity, price, subTotalEls);
                updateSubTotal(quantity, price, subTotalEls)
                
                updateTotal();
                var data = { fid: foodId, quantity: $(this).val() }
                // AJAX Call
                $.ajax({
                    type: "POST",
                    url: "updatecartitem",
                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify(data),
                    dataType: "json",
                    success: function (response) {
                        alert(response)
                    },
                    error: function (response) {
                        console.log(response.responseText)
                        alert(respons.responseTexte)
                    }
                })
            })
        });

        updateTotal();

        function updateSubTotal(quantity, price, subTotalEls) {
            var subTotal = parseInt(quantity) * parseInt(price)
           
            $(subTotalEls).text(subTotal.toLocaleString("vi-VN", {
                style: "currency",
                currency: "VND"
            }))
        }
        function updateTotal() {
            var total = 0;
            $("td[class^='product-subtotal-']").each(function (index, element) {
                total += parseInt($(element).text().replaceAll(".", "").replace("₫", "").trim());
                //console.log($(this).text())
            })
            $(".subtotal").text(total.toLocaleString("vi-VN", {
                style: "currency",
                currency: "VND"
            }));
        }
    })
</script>